import React from "react";
import "./IdleNewExpense.css";

const IdleNewExpense = (props) => {
  console.log(props.onClick);
  return <button type="button"> Add Expense</button>;
};

export default IdleNewExpense;
